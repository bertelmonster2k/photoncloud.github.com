{% if site.ASSET_PATH and site.ASSET_PATH != '' %}
    {% assign ASSET_PATH = site.ASSET_PATH %}
{% else %}
    {% assign ASSET_PATH = '/assets' %}
{% endif %}

{% assign ASSETS = ASSET_PATH %}
{% capture IMG %}{{ ASSET_PATH }}/img{% endcapture %}

{% assign SHARED = '/shared' %}
{% capture SHAREDCSS %}{{ SHARED }}/css{% endcapture %}
{% capture SHAREDIMG %}{{ SHARED }}/img{% endcapture %}
{% capture SHAREDJS %}{{ SHARED }}/js{% endcapture %}